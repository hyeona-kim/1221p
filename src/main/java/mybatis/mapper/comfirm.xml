<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="confirm">
	<select id="search" resultType="mybatis.vo.TrainuploadVO" parameterType="String">
		select * from trainupload_t
		where status = 0
	</select>
	
</mapper>